<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/bootstrap-5.2.3-dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" />

    <style>
        *{
            margin : 0;
            padding : 0;
        }

        form{
            width: 400px;
        }

        .form_edit {
            margin : 50px;
            background-color: #B3C99C;
            margin-left: 450px;
            padding: 10px;
            text-align: center;
            border-radius: 12px;
            box-shadow: 2px 0 4px 2px black;
        }
        body {
            background: url(/images/bg_edit.jpg);
            background-size: auto;
            background-size: cover;
        }
        i{
            font-size: 26px;
        }
        .comeback {
            margin-top: 100px;
            margin-left :220px
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="comeback">
        <h2>Quay lại</h2>
        <a href="/sp/phone">
            <i class="bi bi-arrow-return-left"></i>
        </a>
        </div>
        
        <h2 class="text-center" style="margin-top: -80px;">Sửa sản phẩm</h2>
        <%= msg %>
        <form action="" method="post" class="form_edit">
            <!--  -->
            <div class="boxAdd">
                <div class="mb-3 col-15">
               <label for="nameSp" class="form-label">Tên Sản Phẩm</label>
               <input type="text" class="form-control" id="name" name="name"  value="<%= objSp.name %>">
             </div>
             <div class="mb-3 col-15 ">
               <label for="priceSp" class="form-label">Giá Sản Phẩm</label>
               <input type="text" class="form-control" id="Price" name="Price" value="<%= objSp.Price %>">
             </div>
             <div class="mb-3 col-15">
               <label for="exampleFormControlTextarea1" class="form-label">Chi tiết Sản phẩm</label>
               <textarea class="form-control" id="describe" rows="4"name='describe'><%= objSp.describe %></textarea>
             </div>
             <div class="roleSp dropdown">
                 <select name="roleId" class="dropdown-toggle btn btn-outline-success">
                    <% listRole.forEach(row => { let selected = '  ';
                if( String (row._id) == String (objSp.roleId) ) 
                selected = ' selected ' %>
                    <option <%= selected %> value="<%=row._id %>"> <%= row.roleName %></option>
               <% }) %>
               </select>
             </div>
             <div class="mb-3 col-15 mt-3">
               <label for="imageSp" class="form-label">Ảnh Sản Phẩm</label>
               <input type="text" class="form-control" id="img" name="img" value="<%= objSp.img %>">
             </div>
             </div>
            <button type="submit" class="col-4 btn btn-success">Save</button>
        </form>
    </div>
    <script src="../bootstrap-5.2.3-dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>